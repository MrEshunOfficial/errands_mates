// Basic usage
<AvatarUpdate />

// Advanced usage with callbacks
<AvatarUpdate
  size="large"
  maxSizeInMB={10}
  compressionQuality={0.9}
  onUpdateSuccess={(newUrl) => console.log('Avatar updated:', newUrl)}
  onUpdateError={(error) => console.error('Update failed:', error)}
  showSuccessMessage={true}
  className="my-custom-class"
/>

// Minimal version without header
<AvatarUpdate
  showHeader={false}
  size="small"
  className="compact-avatar-update"
/>